[
    {{- $entries := slice -}}
    {{- range .Site.AllPages -}}
        {{- if and (ne .Kind "section") (ne .Layout "search") (ne .Layout "archives") (ne .Title "") -}}
            {{- $content := .RawContent -}}
            {{- $entry := dict "title" .Title "content" $content "permalink" .Permalink -}}
            {{- $entries = $entries | append $entry -}}
        {{- end -}}
    {{- end -}}
    {{- range $index, $entry := $entries -}}
        {{- if $index -}},{{- end -}}
        {{- $entry | jsonify -}}
    {{- end -}}
]
